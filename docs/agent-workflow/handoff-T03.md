# Task handoff — T03

- Task ID / child ID: T03 — Prove the native toolchain
- Owner / reviewer: agent (role B) / pending human review
- Status: **partial — real dependency scaffold and real failed-build evidence; device acceptance criteria genuinely blocked, not mocked**
- Requirement and acceptance IDs: REQ-028 (per jalsakshi-blueprint/docs/requirements.md); AC-028 real-fresh-agent check not attempted here (out of scope for this task)
- Branch/worktree and base commit, if applicable: base commit `72af7c0` on `main` at session start (T01+T02 commit history may have advanced since; no new branch/worktree used)
- Files changed:
  - `apps/mobile/package.json` (new, generated by `create-expo-app`, then `onnxruntime-react-native` added via real `npm install`)
  - `apps/mobile/package-lock.json` (new, real lockfile — this is the "lockfile" named in task scope)
  - `apps/mobile/app.config.ts` (new, replaces generated `app.json`; adds `android.package` and a D03 comment on why Expo Go won't work)
  - `apps/mobile/App.tsx`, `apps/mobile/index.ts`, `apps/mobile/tsconfig.json`, `apps/mobile/assets/`, `apps/mobile/.gitignore` (new, unmodified template scaffold — not explicitly in task file scope but required for the package.json/app.config.ts to be a working project rather than orphaned config files)
  - `docs/toolchain-matrix.md` (new)
  - `docs/agent-workflow/current-state.md` (updated: T03 active claim, new state, next action)
  - `docs/agent-workflow/handoff-T03.md` (new, this file)
  - Removed (not committed): `apps/mobile/AGENTS.md`, `CLAUDE.md`, `.claude/`, `LICENSE` — generic AI-coding-tips boilerplate shipped by the current `create-expo-app` template, deleted because a second, different-meaning `AGENTS.md` inside `apps/mobile/` would collide with this repo's actual operating contract and could mislead a future agent.
  - Not committed / gitignored: `apps/mobile/node_modules/`, `apps/mobile/android/` (generated by `expo prebuild` during the failed run:android attempt; already covered by the template's own `.gitignore`)
- Inputs, build/device/model/dataset versions:
  - expo 57.0.24, expo-status-bar 57.0.1, onnxruntime-react-native 1.24.3, react 19.2.3, react-native 0.86.3, typescript 6.0.3, @types/react 19.2.18 (all read from the real generated `package-lock.json`, not hand-picked)
  - Build host: Windows 11 / MINGW64, node v24.19.0, npm 12.0.2, java 1.8.0_441
  - Device: **none** — no physical Android phone or emulator available in this environment
- Decisions and authoritative docs updated: none of jalsakshi-blueprint's own docs were edited. Reused D01 (onnxruntime-react-native) and D03 (Expo development builds, not Expo Go) exactly as named in `jalsakshi-blueprint/docs/research/source-register.md`.
- Tests actually run: exact command/procedure; exit/result; timestamp (all 2026-09-22):
  - `npx --yes create-expo-app@latest apps/mobile --template blank-typescript` → succeeded, 467 packages installed.
  - `npm install onnxruntime-react-native` (in `apps/mobile/`) → succeeded, resolved to 1.24.3.
  - `npx expo config --type public` (in `apps/mobile/`) → succeeded; confirmed `app.config.ts` is read correctly (`sdkVersion: '57.0.0'`, `android.package: 'org.jalsakshi.mobile'`).
  - `npx expo run:android` (in `apps/mobile/`) → **failed for real**: prebuild succeeded (generated gitignored `android/`), then `Failed to resolve the Android SDK path` and `Error: 'adb' is not recognized`. This is the literal "failed compatibility stays blocked, not mocked" verification step — the failure was allowed to happen and is recorded, not caught/hidden/downgraded.
- Tests not run and reason: APK install on a real device, and the known-tensor-offline test — both require an Android SDK, `adb`, and a physical/emulated device, none of which exist in this environment. No substitute (e.g. `onnxruntime-node` on the dev machine) was run in their place, because that would validate a different native binding and misrepresent the React Native ONNX bridge as proven when it is not — see rationale in `docs/toolchain-matrix.md`.
- Evidence locations: `docs/toolchain-matrix.md` (full command list and real error text); this file.
- Security/privacy/domain checks: N/A — no PII, no credentials, no secrets involved. No auth/crypto/sync/release code touched.
- Deviations from plan and approved scope changes: user explicitly authorized ("Stay blocked, scaffold config only") building real config/dependency scaffolding while leaving the actual hardware-dependent acceptance criteria unmet, rather than either fabricating device evidence or leaving T03 completely untouched. This is the recorded deviation from "acceptance criteria must all be demonstrably true."
- Remaining issues/risks: this build host also lacks Java 17+, which current Expo/AGP toolchains typically require — a second real gap discovered by the actual failed build, not predicted. Whoever picks up real device testing should expect to hit this too.
- Next exact action and responsible owner: whoever has access to a real Android phone (or emulator) plus a machine with Android SDK/`adb` installed runs `cd apps/mobile && npx expo run:android`, resolves the Java version gap if it recurs, installs the APK, and runs a known ONNX tensor through `onnxruntime-react-native` to actually satisfy T03's acceptance criteria.
- Integration dependencies and shared files needing coordination: `apps/mobile/` is now a real, non-empty directory — any future task touching it (T04, T08, T09, etc.) should build on this scaffold rather than re-running `create-expo-app`, which would prompt to overwrite.

## Completion reminder acknowledgment

This does **not** satisfy T03's real acceptance criteria — APK-on-device and
offline-tensor-parity remain genuinely unmet, not mocked around. What *is*
real here: the dependency versions, the build-host record, and the failed
`run:android` attempt's exact error text. The to-do.md checkbox is **not**
checked by this handoff.
